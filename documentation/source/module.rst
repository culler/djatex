The djatex module
-----------------

The module defines a class, LaTeXFile, and a shortcut function intended to be
called from a django view to render a LaTeX template as an attached PDF file.

.. automodule:: djatex
   :members:

